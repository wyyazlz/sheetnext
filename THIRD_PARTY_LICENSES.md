# Third-Party Licenses

This project depends on third-party open-source packages.

To generate an updated third-party license report in your environment:

```bash
npx license-checker --production --summary
```

Track and archive the resulting report for release compliance.
